# PostgreSQL connection strings (e.g. from Supabase, Neon, Railway, etc.)
DATABASE_URL=
DIRECT_URL=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# No map API key needed: geocoding and venue search use the free
# OpenStreetMap Nominatim API (no signup or billing required).
